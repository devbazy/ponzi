<%= form_for(@old_game) do |f| %>
  <% if @old_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@old_game.errors.count, "error") %> prohibited this old_game from being saved:</h2>

      <ul>
      <% @old_game.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :game_id %><br />
    <%= f.number_field :game_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
